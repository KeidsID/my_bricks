name: fl_app
description: A new brick created with the Mason CLI.
version: 0.1.0

environment:
  mason: ^0.1.0

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, array, or list)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
#  * an optional separator (list only)
vars:
  project-name:
    type: string
    default: my_app
    prompt: What is the name of your project?
  desc:
    type: string
    default: A new Flutter project.
    prompt: How you describe your project?
  org:
    type: string
    default: com.example
    prompt: What is your organization name?
  import-alias:
    type: string
    prompt: Want to modify package import alias? Will use project name by default.
  app-name:
    type: string
    prompt: What is the name of your app? Will use project name by default.
